"""Step 05. 생성.

역할:
    다음 토큰을 예측하고 이어 붙이는 과정을 반복해서 한국어 문장을 생성한다.

학습 포인트:
    - auto-regressive 생성 방식,
    - prompt 인코딩,
    - 다음 토큰 sampling 또는 greedy 선택,
    - 최대 길이 또는 종료 토큰 같은 정지 조건.

나중의 프로젝트 구조:
    이 파일은 `generation.py`가 되거나 추론 서비스 모듈로 분리될 수 있다.
"""
